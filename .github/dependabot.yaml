version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: weekly
      time: "06:00"
  - package-ecosystem: "docker"
    # Look for a `Dockerfile` in each directory
    directories:
      - spoolman
      - bambuddy
      - vbb-rest
    # Check for updates once a week
    schedule:
      interval: "daily"
      time: "06:00"
